/*
package ca.jrvs.practice.codingChallenge;


import ca.jrvs.practice.codingChallenge.DuplicateLinkedListNode.Node;
import com.sun.xml.internal.rngom.parse.compact.Token;
import org.junit.Test;

public class DuplicateLinkedListNodeTest  {

    //private Node node;
    //private Node head;


    @Test
    public void testRemove_duplicate() {
        DuplicateLinkedListNode list = new DuplicateLinkedListNode();
        Node head = new Node();
        list.head = new Node(10);
        list.head.next = new Node(12);
        list.head.next.next = new Node(11);
        list.head.next.next.next = new Node(11);
        list.head.next.next.next.next = new Node(12);
        list.head.next.next.next.next.next = new Node(11);
        list.head.next.next.next.next.next.next = new Node(10);




        System.out.println("Linked List before removing duplicates : \n ");

        list.printList(Node head);

        list.remove_duplicate();
        System.out.println("Linked List after removing duplicates : \n ");

       list.printList(head);

    }
}*/
